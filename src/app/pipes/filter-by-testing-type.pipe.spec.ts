import { FilterByTestingTypePipe } from './filter-by-testing-type.pipe';

describe('FilterByTestingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTestingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
